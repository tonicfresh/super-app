---
phase: 05-test-coverage-documentation
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - template/backend/src/ai/module-integration.integration.test.ts
  - CLAUDE.md
autonomous: true
requirements: [TEST-01, CON-03]

must_haves:
  truths:
    - "E2E Test verdrahtet Module Registry mit AI System und verifiziert Tool-Ausfuehrung"
    - "Permission Check wird im E2E Flow aufgerufen und blockiert bei fehlendem Zugriff"
    - "Cost Logging wird im E2E Flow aufgerufen nach Tool-Ausfuehrung"
    - "CLAUDE.md Implementierungsphasen-Tabelle zeigt Phasen 1-4 als abgeschlossen"
    - "CLAUDE.md Tech Stack spiegelt tatsaechliche package.json Versionen wider"
  artifacts:
    - path: "template/backend/src/ai/module-integration.integration.test.ts"
      provides: "E2E Module Integration Test"
      contains: "Module Integration E2E"
    - path: "CLAUDE.md"
      provides: "Aktualisierte Projekt-Dokumentation"
      contains: "Complete"
  key_links:
    - from: "template/backend/src/ai/module-integration.integration.test.ts"
      to: "template/backend/src/module-registry.ts"
      via: "createModuleRegistry import"
      pattern: "import.*createModuleRegistry"
    - from: "template/backend/src/ai/module-integration.integration.test.ts"
      to: "template/backend/src/ai/index.ts"
      via: "createAISystem import"
      pattern: "import.*createAISystem"
---

<objective>
E2E Module Integration Test (TEST-01) der den kompletten Flow Registry -> AI System -> Tool -> Permission -> Execution -> Cost Logging verdrahtet, plus CLAUDE.md auf aktuellen Code-Stand bringen (CON-03).

Purpose: Der E2E-Test ist der wichtigste Integration Test — er beweist dass alle Einzelkomponenten korrekt zusammenarbeiten. Das CLAUDE.md-Update stellt sicher dass die Dokumentation den Code-Stand nach 4 abgeschlossenen Audit-Phasen korrekt widerspiegelt.
Output: 1 neue Test-Datei, 1 aktualisierte CLAUDE.md
</objective>

<execution_context>
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/05-test-coverage-documentation/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From template/backend/src/ai/test-utils.ts (erstellt in Plan 01):
```typescript
export function createMockModel(): LanguageModel;
export function createTestPlugin(overrides?: Partial<ModulePlugin>): ModulePlugin;
export function createMockAISystemDeps(overrides?: Partial<AISystemDeps>): AISystemDeps;
```

From template/backend/src/module-registry.ts:
```typescript
export interface ModuleRegistry {
  register(plugin: ModulePlugin): void;
  getModule(name: string): RegisteredModule | undefined;
  getAll(): RegisteredModule[];
  getMergedSchema(): Record<string, unknown>;
  getMergedRoutes(): MergedRoute[];
  getMergedJobs(): Array<{ type: string; handler: any }>;
  getAllTools(): Record<string, unknown>;
  getAllPermissions(): Record<string, string[]>;
  getAllGuardrails(): Record<string, GuardrailConfig>;
}

export function createModuleRegistry(): ModuleRegistry;
```

From template/backend/src/ai/index.ts:
```typescript
export interface AISystemDeps { /* ... see Plan 01 interfaces */ }
export interface AISystemConfig {
  chatRoutes: Hono;
  approvalRoutes: Hono;
  approvalManager: ApprovalManager;
  createMainAgent: (userId: string, tenantId: string) => Promise<MainAgentConfig>;
}
export async function createAISystem(deps: AISystemDeps): Promise<AISystemConfig>;
```

From template/backend/src/ai/main-agent.ts:
```typescript
export interface MainAgentConfig {
  tools: Record<string, any>;
  systemPrompt: string;
  model: LanguageModel;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E Module Integration Test (TEST-01)</name>
  <files>template/backend/src/ai/module-integration.integration.test.ts</files>
  <read_first>
    - template/backend/src/ai/test-utils.ts
    - template/backend/src/ai/index.ts
    - template/backend/src/ai/index.test.ts
    - template/backend/src/module-registry.ts
    - template/backend/src/module-registry.test.ts
    - template/backend/src/ai/module-connector.ts
    - template/backend/src/ai/main-agent.ts
  </read_first>
  <behavior>
    Per D-03: Ein einzelner Multi-Step Test der den kompletten Flow abdeckt.

    Szenarien:
    - Test 1: Vollstaendiger E2E Flow — Module registrieren → createAISystem mit Registry → createMainAgent → Tools enthalten Modul-Tools → checkModuleAccess wurde aufgerufen
    - Test 2: Permission Denied — checkModuleAccess gibt false zurueck → Modul-Tools sind NICHT im Agent verfuegbar
    - Test 3: Mehrere Module registriert → AI System erhaelt Tools aller Module (merged)
    - Test 4: Cost Logging — Nach Agent-Erstellung ist logAICost als Dep verdrahtet (verifizierbar ueber Mock-Call-Count nach einer Operation)
  </behavior>
  <action>
    Per D-04: Erstelle `template/backend/src/ai/module-integration.integration.test.ts`.

    Dies ist der komplexeste Test der Phase. Er verdrahtet:
    1. `createModuleRegistry()` — echte Registry, kein Mock
    2. `createTestPlugin()` — aus test-utils.ts, mit echten Tool-Definitionen
    3. `createAISystem()` — echte Funktion mit gemockten DB/Netzwerk-Deps
    4. `system.createMainAgent()` — echte Funktion die Module Connector durchlaeuft

    ```typescript
    import { describe, it, expect, mock, beforeEach } from "bun:test";
    import { createModuleRegistry } from "../module-registry";
    import { createAISystem, type AISystemDeps } from "./index";
    import { createMockModel, createTestPlugin, createMockAISystemDeps } from "./test-utils";
    ```

    Der Test MUSS die echten Funktionen createModuleRegistry und createAISystem verwenden — nur die externen Dependencies (DB, Netzwerk, AI SDK) werden gemockt.

    Fuer den E2E Flow:
    1. Registry erstellen und Plugin registrieren (mit Tool `testTool`)
    2. AISystemDeps erstellen wobei `getRegisteredModules` auf `registry.getAll()` zeigt
    3. `createAISystem(deps)` aufrufen
    4. `system.createMainAgent("user-1", "tenant-1")` aufrufen
    5. Verifizieren: `agent.tools` enthaelt die Modul-Tools
    6. Verifizieren: `deps.checkModuleAccess` wurde aufgerufen

    Fuer Permission Denied:
    1. Selbes Setup, aber `checkModuleAccess: mock(async () => false)`
    2. Nach createMainAgent: `agent.tools` enthaelt KEINE Modul-Tools (nur System-Tools falls vorhanden)

    WICHTIG: Lies zuerst index.ts und module-connector.ts um zu verstehen wie Tools geladen werden. Die loadModuleTools-Funktion ruft checkModuleAccess pro Modul auf. Wenn false, werden die Tools dieses Moduls nicht geladen.

    Describe-Block: "Module Integration E2E" mit Unter-Describes fuer "vollstaendiger Flow", "Permission Enforcement", "Multi-Module".
  </action>
  <verify>
    <automated>cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun test src/ai/module-integration.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Datei `template/backend/src/ai/module-integration.integration.test.ts` existiert
    - Mindestens 4 Test-Cases
    - Importiert `createModuleRegistry` aus "../module-registry"
    - Importiert `createAISystem` aus "./index"
    - Nutzt `createTestPlugin` und `createMockAISystemDeps` aus "./test-utils"
    - Enthaelt `checkModuleAccess` Assertion (Mock-Call verifiziert)
    - Enthaelt Permission-Denied Szenario
    - Alle Tests laufen gruen
  </acceptance_criteria>
  <done>E2E Module Integration Test mit 4+ Tests gruen, vollstaendiger Flow von Registry bis Tool-Ausfuehrung abgedeckt</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md aktualisieren (CON-03)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md
    - template/backend/package.json
    - template/frontend/package.json
    - .planning/ROADMAP.md
    - .planning/phases/04-spec-audit/04-04-SUMMARY.md
  </read_first>
  <action>
    Per D-11, D-12, D-13: Gezieltes Section-Update in drei Bereichen. KEIN komplettes Rewrite.

    **Sektion 1: Implementierungsphasen Status-Tabelle**
    Die Tabelle unter "## Implementierungsphasen" aktualisieren:
    - Phase 1-4: Status-Spalte von "Plan fertig" zu Beschreibung des tatsaechlichen Zustands basierend auf den Audit-Ergebnissen aus Phase 04. Lies 04-04-SUMMARY.md fuer die konsolidierten Ergebnisse.
    - Korrekte Status-Bezeichnungen verwenden: z.B. "Implementiert", "Teilweise", "Plan fertig"
    - Phase 5-8: Unveraendert lassen ("Plan fertig")

    **Sektion 2: Tech Stack Versionen**
    Die Tabellen unter "## Tech Stack" pruefen und bei Abweichungen aktualisieren:
    - Backend-Versionen gegen `template/backend/package.json` abgleichen
    - Frontend-Versionen gegen `template/frontend/package.json` abgleichen
    - Nur Versionen aendern die tatsaechlich abweichen (Phase 1 hat Version-Sync durchgefuehrt)

    **Sektion 3: Projektstruktur**
    Den Baum unter "## Projektstruktur" pruefen:
    - Neue Verzeichnisse/Dateien die in Phasen 1-4 hinzugekommen sind (z.B. neue Service-Dateien, Test-Utilities)
    - Falls die Struktur im Wesentlichen korrekt ist, nur ergaenzen, nicht umschreiben

    NICHT aendern: Architecture-Sektionen, Conventions-Sektionen, Verbindungen, Entwicklungsumgebung (per D-12).

    Verwende das Edit-Tool fuer gezielte Aenderungen, NICHT Write fuer die gesamte Datei.
  </action>
  <verify>
    <automated>grep -c "Complete\|Implementiert\|Teilweise" /Users/toby/Documents/github/projekte/super-app/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Implementierungsphasen-Tabelle enthaelt aktualisierte Status fuer Phase 1-4
    - Tech Stack Versionen stimmen mit package.json ueberein (stichprobenartig: Hono, Vue, Drizzle)
    - Keine Aenderungen in Architecture- oder Conventions-Sektionen
    - Datei ist weiterhin valides Markdown
  </acceptance_criteria>
  <done>CLAUDE.md spiegelt den tatsaechlichen Code-Stand wider: Phasen-Status aktualisiert, Tech Stack Versionen synchronisiert, Projektstruktur ergaenzt</done>
</task>

</tasks>

<verification>
```bash
# E2E Test gruen
cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun test src/ai/module-integration.integration.test.ts

# Gesamte Test-Suite gruen (Phase-Gate)
cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun test

# CLAUDE.md Phasen-Status aktualisiert
grep -A2 "Phase 1" /Users/toby/Documents/github/projekte/super-app/CLAUDE.md | head -5
```
</verification>

<success_criteria>
- E2E Module Integration Test: 4+ Tests gruen, Registry -> AI System -> Tool -> Permission Flow komplett abgedeckt
- CLAUDE.md: Phasen 1-4 Status aktualisiert, Tech Stack Versionen synchronisiert
- Gesamte Test-Suite (`bun test`): Alle Tests gruen (bestehende 71 + neue Integration Tests)
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-documentation/05-03-SUMMARY.md`
</output>
