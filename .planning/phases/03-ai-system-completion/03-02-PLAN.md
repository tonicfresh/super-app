---
phase: 03-ai-system-completion
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - template/backend/src/index.ts
  - template/backend/src/ai/main-agent.ts
  - template/backend/src/ai/index.ts
  - template/backend/src/db/schema.ts
autonomous: true
requirements: [SEC-03, SEC-04, AI-05]

must_haves:
  truths:
    - "Approval Requests werden in app_approval_requests DB-Tabelle persistiert wenn AI-Tool Approval braucht"
    - "Agent Steps werden kumulativ in mc_agent_sessions geloggt (Session-Level, nicht per-Step)"
    - "Rate Limiter ist auf sensitive Endpoints (Approval-Routes, Chat) aktiviert"
    - "sessionId wird einmalig in createMainAgent generiert und per Closure durch alle Steps weitergegeben"
  artifacts:
    - path: "template/backend/src/index.ts"
      provides: "Verdrahtung der drei Stubs (logAgentStepToDB, storeApprovalRequest, updateApprovalRequest)"
      contains: "createAgentSessionTracker"
    - path: "template/backend/src/ai/main-agent.ts"
      provides: "sessionId-Generierung und isFirstStep-Tracking per Closure"
      contains: "randomUUID"
    - path: "template/backend/src/db/schema.ts"
      provides: "Re-export des Approval-Schemas fuer Migration"
      contains: "appApprovalRequests"
  key_links:
    - from: "template/backend/src/index.ts"
      to: "template/backend/src/ai/agent-session-tracker.ts"
      via: "createAgentSessionTracker(getDb())"
      pattern: "createAgentSessionTracker"
    - from: "template/backend/src/index.ts"
      to: "template/backend/src/db/approval-requests.schema.ts"
      via: "import appApprovalRequests fuer DB-Insert/Update"
      pattern: "appApprovalRequests"
    - from: "template/backend/src/ai/main-agent.ts"
      to: "template/backend/src/ai/agent-session-tracker.ts"
      via: "AgentStepLogWithSession durchgereicht via logAgentStep"
      pattern: "sessionId.*isFirstStep"
    - from: "template/backend/src/index.ts"
      to: "template/backend/src/middleware/rate-limiter.ts"
      via: "sensitiveRateLimiter auf Approval/Chat Routes"
      pattern: "sensitiveRateLimiter"
---

<objective>
Die drei verbleibenden TODO-Stubs in index.ts verdrahten und sessionId-Closure in createMainAgent() einbauen. Rate Limiter auf sensitive Endpoints aktivieren. Approval-Schema in DB-Migrations registrieren.

Purpose: Nach diesem Plan sind alle drei Phase-3 Requirements (SEC-03, SEC-04, AI-05) vollstaendig implementiert — keine Stubs mehr.
Output: 4 modifizierte Dateien, funktionierende End-to-End-Verdrahtung
</objective>

<execution_context>
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-system-completion/03-CONTEXT.md
@.planning/phases/03-ai-system-completion/03-RESEARCH.md
@.planning/phases/03-ai-system-completion/03-01-SUMMARY.md

<interfaces>
<!-- Von Plan 01 erstellte Interfaces — Executor muss diese kennen -->

From template/backend/src/ai/agent-session-tracker.ts (Plan 01):
```typescript
export interface AgentStepLogWithSession extends AgentStepLog {
  sessionId: string;
  isFirstStep: boolean;
  moduleName?: string;
  channel?: string;
}

export function createAgentSessionTracker(db: PostgresJsDatabase<any>):
  (step: AgentStepLogWithSession) => Promise<void>;
```

From template/backend/src/db/approval-requests.schema.ts (Plan 01):
```typescript
export const appApprovalRequests = appTable("approval_requests", {
  id: text("id").primaryKey(),
  tenantId: text("tenant_id").notNull(),
  userId: text("user_id").notNull(),
  toolName: text("tool_name").notNull(),
  toolArgs: jsonb("tool_args").notNull().default({}),
  status: text("status").notNull().default("pending"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  resolvedAt: timestamp("resolved_at"),
  resolvedBy: text("resolved_by"),
});
export type AppApprovalRequestInsert = typeof appApprovalRequests.$inferInsert;
export type AppApprovalRequestSelect = typeof appApprovalRequests.$inferSelect;
```

From template/backend/src/middleware/rate-limiter.ts (Plan 01):
```typescript
export function createRateLimiter(config: RateLimiterConfig): MiddlewareHandler;
export const standardRateLimiter: MiddlewareHandler;
export const sensitiveRateLimiter: MiddlewareHandler;
```

<!-- Bestehende Interfaces die modifiziert werden -->

From template/backend/src/ai/main-agent.ts (Zeilen 51-72):
```typescript
export interface MainAgentDeps {
  loadModuleTools: (userId: string, tenantId: string) => Promise<Record<string, unknown>>;
  loadSubAgents: (userId: string, tenantId: string) => Promise<Record<string, unknown>>;
  createDynamicAgentTool: () => unknown;
  model: LanguageModel;
  logAICost: (entry: Partial<AICostEntry>) => Promise<void>;
  logAgentStep: (step: AgentStepLog) => Promise<void>;
  isDebugEnabled: () => Promise<boolean>;
}
```

From template/backend/src/index.ts (Zeilen 154-165 — die Stubs):
```typescript
logAgentStepToDB: async (_step) => {
  // TODO: Insert in mc_agent_sessions Tabelle
},
storeApprovalRequest: async (_request) => {
  // TODO: Insert in approval_requests Tabelle
},
updateApprovalRequest: async (_id, _update) => {
  // TODO: Update in approval_requests Tabelle
},
notifyUser: async (_userId, _data) => {
  // TODO: WebSocket + Push Notification
},
```

From template/backend/src/ai/index.ts (Zeile 97):
```typescript
logAgentStep: deps.logAgentStepToDB,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sessionId-Closure in createMainAgent und logAgentStep-Signatur anpassen</name>
  <files>
    template/backend/src/ai/main-agent.ts,
    template/backend/src/ai/index.ts
  </files>
  <action>
    **main-agent.ts aendern (per D-08):**

    1. Import `randomUUID` von `crypto` hinzufuegen (oder `crypto.randomUUID` direkt)
    2. Import `AgentStepLogWithSession` aus `./agent-session-tracker`
    3. `MainAgentDeps.logAgentStep` Signatur aendern von `(step: AgentStepLog) => Promise<void>` zu `(step: AgentStepLogWithSession) => Promise<void>`
    4. In `createMainAgent()` Funktion:
       - `const sessionId = crypto.randomUUID();` am Anfang der Funktion (einmalig pro Agent-Aufruf)
       - `let isFirstStep = true;` als Closure-Variable
    5. In `onStepFinish` Callback (Zeile 152): `deps.logAgentStep()` Aufruf erweitern:
       ```typescript
       await deps.logAgentStep({
         sessionId,           // Neu
         isFirstStep,         // Neu
         userId,
         tenantId,
         agentType: "main",
         stepNumber,
         tokensInput: usage.promptTokens,
         tokensOutput: usage.completionTokens,
         toolCalls: toolCalls.map((tc) => ({ name: tc.name, args: tc.args })),
         finishReason,
         timestamp: new Date(),
       });
       isFirstStep = false;   // Nach erstem Step umschalten
       ```

    **index.ts (Zeile 97):** Keine Aenderung noetig — `deps.logAgentStepToDB` wird bereits als `logAgentStep` weitergereicht. Die Signatur-Aenderung in MainAgentDeps reicht.

    Variablen Englisch, Kommentare Deutsch.
  </action>
  <verify>
    <automated>cd /Users/toby/Documents/github/projekte/super-app && bun run type-check 2>&1 | head -20; echo "---"; bun test template/backend/src/ai/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    - sessionId wird einmalig in createMainAgent() generiert (nicht pro Step)
    - isFirstStep ist true beim ersten Step, danach false
    - logAgentStep empfaengt AgentStepLogWithSession (mit sessionId + isFirstStep)
    - Bestehende Tests laufen weiter
  </done>
</task>

<task type="auto">
  <name>Task 2: Stubs in index.ts verdrahten, Schema registrieren, Rate Limiter aktivieren</name>
  <files>
    template/backend/src/index.ts,
    template/backend/src/db/schema.ts
  </files>
  <action>
    **schema.ts erweitern:**
    Re-Export des neuen Approval-Schemas hinzufuegen damit Drizzle-Kit es fuer Migrations erkennt:
    ```typescript
    export { appApprovalRequests } from "./approval-requests.schema";
    export type { AppApprovalRequestInsert, AppApprovalRequestSelect } from "./approval-requests.schema";
    ```

    **index.ts — Imports hinzufuegen:**
    ```typescript
    import { createAgentSessionTracker } from "./ai/agent-session-tracker";
    import { appApprovalRequests } from "./db/approval-requests.schema";
    import { sensitiveRateLimiter } from "./middleware/rate-limiter";
    import { eq } from "drizzle-orm";
    ```

    **index.ts — logAgentStepToDB Stub ersetzen (Zeile 154-156):**
    ```typescript
    logAgentStepToDB: createAgentSessionTracker(getDb()),
    ```
    Nutzt die Lazy-Init `getDb()` Funktion (Pattern aus Phase 02, per Decision "Lazy cost queries").

    **index.ts — storeApprovalRequest Stub ersetzen (Zeile 157-159):**
    ```typescript
    storeApprovalRequest: async (request) => {
      const db = getDb();
      await db.insert(appApprovalRequests).values({
        id: request.id,
        tenantId: request.tenantId,
        userId: request.userId,
        toolName: request.toolName,
        toolArgs: request.toolArgs,
        status: request.status,
        createdAt: request.createdAt,
      });
    },
    ```

    **index.ts — updateApprovalRequest Stub ersetzen (Zeile 160-162):**
    ```typescript
    updateApprovalRequest: async (id, update) => {
      const db = getDb();
      await db.update(appApprovalRequests)
        .set({
          status: update.status,
          resolvedAt: update.resolvedAt,
          resolvedBy: update.resolvedBy,
        })
        .where(eq(appApprovalRequests.id, id));
    },
    ```
    WICHTIG: `resolvedBy` kommt aus dem Auth-Context (wird von ApprovalManager per Hono userId gesetzt) — nicht aus dem Request-Body (per Pitfall 4 aus Research).

    **index.ts — notifyUser minimale Implementierung:**
    ```typescript
    notifyUser: async (userId, request) => {
      // Minimaler Log — vollstaendige Push/SSE Integration in spaeterer Phase
      console.log(`[approval] Approval angefragt fuer User ${userId}: ${request.toolName}`);
    },
    ```
    Vollstaendige Push/SSE-Integration (emitActivity, sendPushNotification) wird als v2 behandelt — der activityBus ist in index.ts nicht direkt verfuegbar ohne Refactoring der defineServer-Struktur. Der Log reicht fuer Phase 3 Scope.

    **index.ts — Rate Limiter aktivieren:**
    Pruefe ob `defineServer` einen Hook fuer globale Middleware hat oder ob die App-Instanz nach `defineServer()` zugaenglich ist. Falls ja:
    - `sensitiveRateLimiter` auf Approval-Routes und Chat-Routes anwenden
    - Falls `defineServer` keinen direkten Middleware-Hook bietet: Rate Limiter als Kommentar mit TODO dokumentieren und stattdessen in den bestehenden Route-Definitionen (z.B. `createChatRoutes`) als Route-Level Middleware einhaengen

    Pragmatischer Ansatz: Wenn der `server` (Hono-App) nach `defineServer()` zurueckgegeben wird, `server.use("/api/v1/tenant/*/chat/*", sensitiveRateLimiter)` hinzufuegen. Wenn nicht: Rate Limiter Export ist bereit fuer spaetere Route-Level-Integration — als TODO markieren mit Verweis auf die exportierten Instanzen.

    Variablen Englisch, Kommentare Deutsch.
  </action>
  <verify>
    <automated>cd /Users/toby/Documents/github/projekte/super-app && bun test template/backend/src/ 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Alle 3 TODO-Stubs in index.ts durch echte Implementierungen ersetzt
    - appApprovalRequests in schema.ts re-exportiert (Migration-faehig)
    - logAgentStepToDB nutzt createAgentSessionTracker mit getDb()
    - storeApprovalRequest/updateApprovalRequest nutzen Drizzle insert/update
    - Rate Limiter ist aktiviert oder als TODO mit konkretem Code dokumentiert
    - Keine bestehenden Tests gebrochen
  </done>
</task>

</tasks>

<verification>
- Keine TODO-Stubs mehr in index.ts (Zeilen 154-165 durch echte Implementierungen ersetzt)
- `bun test template/backend/src/` laeuft vollstaendig gruen
- `grep -n "TODO.*mc_agent_sessions\|TODO.*approval_requests" template/backend/src/index.ts` liefert 0 Treffer
- appApprovalRequests Schema ist in schema.ts re-exportiert
- sessionId wird in createMainAgent() per crypto.randomUUID() generiert
</verification>

<success_criteria>
- SEC-03: storeApprovalRequest schreibt in app_approval_requests, updateApprovalRequest aktualisiert Status
- SEC-04: sensitiveRateLimiter ist verfuegbar und auf sensitive Endpoints aktiviert (oder als TODO mit Code bereit)
- AI-05: logAgentStepToDB nutzt createAgentSessionTracker mit INSERT/UPDATE-Pattern gegen mc_agent_sessions
- Alle bestehenden Tests gruen
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-system-completion/03-02-SUMMARY.md`
</output>
