---
phase: 05-test-coverage-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - template/backend/src/ai/test-utils.ts
  - template/backend/src/ai/cost-guardrail.integration.test.ts
  - template/backend/src/ai/privacy-integration.test.ts
autonomous: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "Cost Guardrail blockiert Tool-Ausfuehrung wenn Tagesbudget ueberschritten"
    - "Cost Guardrail blockiert bei Per-Call Maximum Ueberschreitung"
    - "Cost Guardrail blockiert bei Modul-Tageslimit Ueberschreitung"
    - "Privacy System erkennt und maskiert sensible Daten in AI-Tool-Kontext"
    - "Sanitized Tool-Response enthaelt keine Klartext-Emails, Telefonnummern oder API-Keys"
  artifacts:
    - path: "template/backend/src/ai/test-utils.ts"
      provides: "Gemeinsame Test-Fixtures und Mocks fuer Integration Tests"
      exports: ["createMockModel", "createTestPlugin", "createMockAISystemDeps"]
    - path: "template/backend/src/ai/cost-guardrail.integration.test.ts"
      provides: "Cost Guardrail Integration Tests"
      contains: "DAILY_BUDGET_EXCEEDED"
    - path: "template/backend/src/ai/privacy-integration.test.ts"
      provides: "Privacy Integration Tests (erweitert)"
      contains: "AI Tool Kontext Privacy"
  key_links:
    - from: "template/backend/src/ai/cost-guardrail.integration.test.ts"
      to: "template/backend/src/ai/cost-guardrails.ts"
      via: "createCostGuardrailChecker import"
      pattern: "import.*createCostGuardrailChecker"
    - from: "template/backend/src/ai/privacy-integration.test.ts"
      to: "template/backend/src/ai/privacy.ts"
      via: "containsSensitiveData + sanitizeToolResponse import"
      pattern: "import.*sanitizeToolResponse"
---

<objective>
Gemeinsame Test-Utilities erstellen und zwei fokussierte Integration Tests implementieren: Cost Guardrail Enforcement (TEST-03) und Privacy System (TEST-04).

Purpose: Kritische Sicherheits- und Kosten-Schutzmechanismen durch automatisierte Tests absichern. Die test-utils.ts stellt wiederverwendbare Fixtures fuer alle Phase-5 Tests bereit.
Output: 3 Dateien — test-utils.ts, cost-guardrail.integration.test.ts, erweiterte privacy-integration.test.ts
</objective>

<execution_context>
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From template/backend/src/ai/cost-guardrails.ts:
```typescript
export interface CostGuardrailConfig {
  dailyBudgetUsd: number;
  perCallMaxUsd: number;
  perModuleDailyUsd: number;
}

export const DEFAULT_COST_GUARDRAILS: CostGuardrailConfig = {
  dailyBudgetUsd: 5.0,
  perCallMaxUsd: 0.5,
  perModuleDailyUsd: 2.0,
};

export interface CostGuardrailResult {
  allowed: boolean;
  reason?: "DAILY_BUDGET_EXCEEDED" | "PER_CALL_MAX_EXCEEDED" | "MODULE_DAILY_LIMIT_EXCEEDED";
  dailySpent: number;
  dailyBudget: number;
  dailyRemaining: number;
  moduleSpent: number;
  moduleLimit: number;
  moduleRemaining: number;
}

export interface CostGuardrailDeps {
  getConfig: () => Promise<CostGuardrailConfig>;
  getDailyTotalUsd: () => Promise<number>;
  getModuleDailyUsd: (moduleName: string) => Promise<number>;
}

export function createCostGuardrailChecker(deps: CostGuardrailDeps): {
  check(moduleName: string, estimatedCostUsd: number): Promise<CostGuardrailResult>;
};
```

From template/backend/src/ai/privacy.ts:
```typescript
export type SensitiveDataType = "email" | "phone" | "password" | "apiKey";
export function containsSensitiveData(data: unknown, currentPath?: string): SensitiveDataResult;
export function sanitizeToolResponse(response: any): any;
```

From template/backend/src/ai/index.ts:
```typescript
export interface AISystemDeps {
  getRegisteredModules: () => ModulePlugin[];
  checkModuleAccess: (userId: string, tenantId: string, moduleName: string) => Promise<boolean>;
  model: LanguageModel;
  logAICost: (entry: Partial<AICostEntry>) => Promise<void>;
  logAgentStepToDB: (step: AgentStepLog) => Promise<void>;
  storeApprovalRequest: (request: any) => Promise<void>;
  updateApprovalRequest: (id: string, update: any) => Promise<void>;
  notifyUser: (userId: string, data: any) => Promise<void>;
  loadApprovalRequest?: (requestId: string) => Promise<ApprovalRequest | null>;
  isDebugEnabled: () => Promise<boolean>;
}
```

From @super-app/shared (ModulePlugin):
```typescript
export interface ModuleConfig {
  name: string;
  version: string;
  permissions: {
    base: { read: string; write: string; update: string; delete: string };
    custom?: Record<string, string>;
  };
  guardrails?: Record<string, GuardrailConfig>;
}

export interface ModulePlugin {
  config: ModuleConfig;
  schema?: Record<string, unknown>;
  routes?: (app: any) => void;
  jobs?: Array<{ type: string; handler: any }>;
  tools?: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gemeinsame Test-Utilities erstellen</name>
  <files>template/backend/src/ai/test-utils.ts</files>
  <read_first>
    - template/backend/src/ai/main-agent.test.ts
    - template/backend/src/ai/index.test.ts
    - template/backend/src/module-registry.test.ts
    - template/backend/src/ai/index.ts
  </read_first>
  <action>
    Erstelle `template/backend/src/ai/test-utils.ts` mit drei Factory-Funktionen fuer wiederverwendbare Test-Fixtures:

    1. `createMockModel()` — Gibt ein LanguageModel-Mock-Objekt zurueck. Pattern aus main-agent.test.ts uebernehmen:
       ```typescript
       import type { LanguageModel } from "ai";
       export function createMockModel(): LanguageModel {
         return { modelId: "test-model", provider: "test-provider" } as unknown as LanguageModel;
       }
       ```

    2. `createTestPlugin(overrides?)` — Gibt ein valides ModulePlugin zurueck das die Valibot-Validation besteht. Basis-Struktur aus module-registry.test.ts uebernehmen (mailPlugin-Fixture). Overrides via Spread-Operator mergen. WICHTIG: Muss alle Pflichtfelder von ModulePluginSchema enthalten (name, version, permissions.base mit read/write/update/delete).
       ```typescript
       import type { ModulePlugin, ModuleConfig } from "@super-app/shared";
       export function createTestPlugin(overrides?: Partial<ModulePlugin>): ModulePlugin {
         const defaultConfig: ModuleConfig = {
           name: "test-module",
           version: "1.0.0",
           permissions: {
             base: { read: "test:read", write: "test:write", update: "test:update", delete: "test:delete" },
           },
         };
         return {
           config: defaultConfig,
           schema: {},
           routes: (_app: any) => {},
           jobs: [],
           tools: {},
           ...overrides,
         };
       }
       ```

    3. `createMockAISystemDeps(overrides?)` — Gibt ein AISystemDeps-Objekt mit Mock-Defaults zurueck. Alle Funktionen gemockt mit sinnvollen Defaults (keine No-Ops die Fehler werfen). Nutze `mock()` aus `bun:test`.
       ```typescript
       import { mock } from "bun:test";
       import type { AISystemDeps } from "./index";
       export function createMockAISystemDeps(overrides?: Partial<AISystemDeps>): AISystemDeps {
         return {
           getRegisteredModules: mock(() => []),
           checkModuleAccess: mock(async () => true),
           model: createMockModel(),
           logAICost: mock(async () => {}),
           logAgentStepToDB: mock(async () => {}),
           storeApprovalRequest: mock(async () => {}),
           updateApprovalRequest: mock(async () => {}),
           notifyUser: mock(async () => {}),
           isDebugEnabled: mock(async () => false),
           ...overrides,
         };
       }
       ```

    Alle Exports als named exports. Keine default exports.
  </action>
  <verify>
    <automated>cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Datei `template/backend/src/ai/test-utils.ts` existiert
    - Enthaelt `export function createMockModel`
    - Enthaelt `export function createTestPlugin`
    - Enthaelt `export function createMockAISystemDeps`
    - Importiert `LanguageModel` aus "ai"
    - Importiert `ModulePlugin` aus "@super-app/shared"
    - Importiert `AISystemDeps` aus "./index"
    - Keine TypeScript-Fehler
  </acceptance_criteria>
  <done>test-utils.ts existiert mit 3 Factory-Funktionen, TypeScript kompiliert fehlerfrei</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cost Guardrail Integration Test (TEST-03)</name>
  <files>template/backend/src/ai/cost-guardrail.integration.test.ts</files>
  <read_first>
    - template/backend/src/ai/cost-guardrails.ts
    - template/backend/src/ai/cost-tracking.test.ts
    - template/backend/src/ai/test-utils.ts
  </read_first>
  <behavior>
    Per D-07: Integration Test der prueft dass Tool-Ausfuehrung blockiert wird wenn Budget ueberschritten.

    Szenarien:
    - Test 1: Call erlaubt wenn alle Budgets im Rahmen (dailyTotal=0, moduleCost=0, estimatedCost=0.01) → allowed=true
    - Test 2: Call blockiert bei Tagesbudget-Ueberschreitung (dailyTotal=4.90, estimatedCost=0.20 > 5.0 Budget) → allowed=false, reason="DAILY_BUDGET_EXCEEDED"
    - Test 3: Call blockiert bei Per-Call Maximum (estimatedCost=0.60 > 0.50 perCallMax) → allowed=false, reason="PER_CALL_MAX_EXCEEDED"
    - Test 4: Call blockiert bei Modul-Tageslimit (moduleDaily=1.90, estimatedCost=0.20 > 2.0 Limit) → allowed=false, reason="MODULE_DAILY_LIMIT_EXCEEDED"
    - Test 5: Call erlaubt exakt am Budget-Rand (dailyTotal=4.50, estimatedCost=0.50 = 5.0 genau) → allowed=true
    - Test 6: Checker gibt korrekte Statistik-Werte zurueck (dailySpent, dailyRemaining, moduleSpent, moduleRemaining)
  </behavior>
  <action>
    Per D-08: Erstelle `template/backend/src/ai/cost-guardrail.integration.test.ts`.

    Pattern: `createCostGuardrailChecker(mockDeps)` mit verschiedenen Budget-Szenarien.

    ```typescript
    import { describe, it, expect, mock } from "bun:test";
    import { createCostGuardrailChecker, DEFAULT_COST_GUARDRAILS } from "./cost-guardrails";
    ```

    Fuer jeden Test: Eigene Mock-Deps mit spezifischen Werten fuer getDailyTotalUsd und getModuleDailyUsd. getConfig gibt immer DEFAULT_COST_GUARDRAILS zurueck.

    WICHTIG: Die Checker-Logik prueft `dailyTotal + estimatedCost > dailyBudget` (strikt groesser). Daher ist 4.50 + 0.50 = 5.0 NICHT groesser als 5.0 → erlaubt.

    Describe-Block: "Cost Guardrail Integration" mit Unter-Describes fuer "erlaubte Calls" und "blockierte Calls".
  </action>
  <verify>
    <automated>cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun test src/ai/cost-guardrail.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Datei `template/backend/src/ai/cost-guardrail.integration.test.ts` existiert
    - Mindestens 6 Test-Cases (describe/it Bloecke)
    - Enthaelt `DAILY_BUDGET_EXCEEDED` String-Literal
    - Enthaelt `PER_CALL_MAX_EXCEEDED` String-Literal
    - Enthaelt `MODULE_DAILY_LIMIT_EXCEEDED` String-Literal
    - Importiert `createCostGuardrailChecker` aus "./cost-guardrails"
    - Alle Tests laufen gruen
  </acceptance_criteria>
  <done>6 Integration Tests fuer Cost Guardrails laufen gruen, alle drei Blockierungs-Szenarien abgedeckt</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Privacy System Integration Test erweitern (TEST-04)</name>
  <files>template/backend/src/ai/privacy-integration.test.ts</files>
  <read_first>
    - template/backend/src/ai/privacy-integration.test.ts
    - template/backend/src/ai/privacy.ts
    - template/backend/src/ai/test-utils.ts
  </read_first>
  <behavior>
    Per D-09: Integration Test der einen AI-Tool-Aufruf simuliert und verifiziert dass der LLM-Kontext nur maskierte Daten enthaelt.

    WICHTIG: Die bestehende privacy-integration.test.ts hat bereits 10 Tests in 3 describe-Bloecken. Diese NICHT veraendern oder loeschen. Neue Tests am Ende als NEUEN describe-Block hinzufuegen.

    Neue Szenarien:
    - Test 1: Tool-Response mit Email wird vor LLM-Weitergabe automatisch sanitized → kein Klartext-Email im Output
    - Test 2: Tool-Response mit verschachtelten sensiblen Daten (Email in Array von Objekten) wird vollstaendig sanitized
    - Test 3: Tool-Response ohne sensible Daten passiert sanitizeToolResponse unveraendert durch
    - Test 4: Kombinierter Flow — Tool gibt Daten zurueck, containsSensitiveData erkennt Problem, sanitizeToolResponse bereinigt, Re-Check bestaetigt Bereinigung
  </behavior>
  <action>
    Per D-10: Erweitere `template/backend/src/ai/privacy-integration.test.ts`.

    BESTEHENDE Tests (Zeilen 1-184) NICHT veraendern. Fuege am Ende der Datei einen neuen describe-Block hinzu:

    ```typescript
    describe("AI Tool Kontext Privacy", () => {
      // Simuliert den Flow: Tool gibt Daten zurueck → Check → Sanitize → LLM bekommt nur bereinigte Daten
    });
    ```

    Teste den vollstaendigen Privacy-Flow wie er im AI-System ablaufen wuerde:
    1. Ein Mock-Tool gibt eine Response mit sensiblen Daten zurueck
    2. `containsSensitiveData()` wird aufgerufen und erkennt das Problem
    3. `sanitizeToolResponse()` wird aufgerufen und bereinigt die Daten
    4. Der bereinigte Output wird nochmals mit `containsSensitiveData()` geprueft → `found: false`

    Nutze realistische Daten: Deutsche Telefonnummern (+49 ...), deutsche Email-Adressen, API-Keys im sk_-Format.

    NICHT importieren: Die bestehenden Imports am Dateianfang reichen (containsSensitiveData, sanitizeToolResponse aus "./privacy").
  </action>
  <verify>
    <automated>cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun test src/ai/privacy-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Datei `template/backend/src/ai/privacy-integration.test.ts` enthaelt die bestehenden 10 Tests PLUS mindestens 4 neue Tests
    - Neuer describe-Block "AI Tool Kontext Privacy" existiert
    - Bestehende describe-Bloecke "Tool response patterns" und "Sanitization end-to-end" sind unveraendert
    - Enthaelt `containsSensitiveData` UND `sanitizeToolResponse` Aufrufe im neuen Block
    - Alle Tests (alte + neue) laufen gruen
  </acceptance_criteria>
  <done>Privacy Integration Tests erweitert um AI-Tool-Kontext-Szenarien, alle 14+ Tests gruen</done>
</task>

</tasks>

<verification>
```bash
cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun test src/ai/cost-guardrail.integration.test.ts src/ai/privacy-integration.test.ts
```
Beide Test-Dateien laufen gruen. Bestehende Tests nicht gebrochen:
```bash
cd /Users/toby/Documents/github/projekte/super-app/template/backend && bun test
```
</verification>

<success_criteria>
- test-utils.ts mit 3 Factory-Funktionen existiert und kompiliert
- Cost Guardrail Integration Test: 6 Tests gruen, alle 3 Blockierungs-Szenarien abgedeckt
- Privacy Integration Test: 14+ Tests gruen (10 bestehende + 4 neue)
- Gesamte Test-Suite (`bun test`) laeuft ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-documentation/05-01-SUMMARY.md`
</output>
