---
phase: 03-ai-system-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - template/backend/src/db/approval-requests.schema.ts
  - template/backend/src/ai/agent-session-tracker.ts
  - template/backend/src/ai/agent-session-tracker.test.ts
  - template/backend/src/middleware/rate-limiter.ts
  - template/backend/src/middleware/rate-limiter.test.ts
autonomous: true
requirements: [SEC-03, SEC-04, AI-05]

must_haves:
  truths:
    - "Approval-Request-Schema existiert mit app_* Prefix und allen Feldern per D-01"
    - "Agent Session Tracker fuehrt INSERT beim ersten Step und UPDATE bei Folge-Steps aus"
    - "Rate Limiter blockiert Requests nach Limit mit HTTP 429 und Retry-After Header"
  artifacts:
    - path: "template/backend/src/db/approval-requests.schema.ts"
      provides: "Drizzle schema fuer app_approval_requests Tabelle"
      exports: ["appApprovalRequests", "AppApprovalRequestInsert", "AppApprovalRequestSelect"]
    - path: "template/backend/src/ai/agent-session-tracker.ts"
      provides: "createAgentSessionTracker Factory mit INSERT/UPDATE Logik"
      exports: ["createAgentSessionTracker", "AgentStepLogWithSession"]
    - path: "template/backend/src/middleware/rate-limiter.ts"
      provides: "Sliding-Window Rate Limiter Hono Middleware"
      exports: ["createRateLimiter", "standardRateLimiter", "sensitiveRateLimiter"]
  key_links:
    - from: "template/backend/src/ai/agent-session-tracker.ts"
      to: "modules/mission-control/backend/src/db/schema.ts"
      via: "import mcAgentSessions"
      pattern: "mcAgentSessions"
    - from: "template/backend/src/ai/agent-session-tracker.ts"
      to: "template/backend/src/ai/main-agent.ts"
      via: "extends AgentStepLog interface"
      pattern: "AgentStepLogWithSession extends AgentStepLog"
---

<objective>
Neue Dateien fuer Approval-Schema, Agent-Session-Tracker und Rate-Limiter erstellen — alle drei sind unabhaengig voneinander und werden in Plan 02 verdrahtet.

Purpose: Fundamente legen, bevor die Verdrahtung in index.ts und main-agent.ts erfolgt. Jede Datei ist eigenstaendig testbar.
Output: 3 neue Produktionsdateien + 2 Testdateien
</objective>

<execution_context>
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toby/Documents/github/projekte/super-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-system-completion/03-CONTEXT.md
@.planning/phases/03-ai-system-completion/03-RESEARCH.md

<interfaces>
<!-- Bestehende Interfaces die der Executor braucht -->

From template/backend/src/ai/main-agent.ts:
```typescript
export interface AgentStepLog {
  userId: string;
  tenantId: string;
  agentType: "main" | "sub" | "dynamic";
  stepNumber: number;
  tokensInput: number;
  tokensOutput: number;
  toolCalls: Array<{
    name: string;
    args: Record<string, unknown>;
  }>;
  finishReason: string;
  timestamp: Date;
}
```

From modules/mission-control/backend/src/db/schema.ts:
```typescript
export const mcAgentSessions = mcTable("agent_sessions", {
  id: text("id").primaryKey(),
  agentType: text("agent_type").notNull(),
  moduleName: text("module_name").notNull(),
  userId: text("user_id").notNull(),
  channel: text("channel").notNull(),
  status: text("status").notNull().default("running"),
  startedAt: timestamp("started_at").notNull().defaultNow(),
  completedAt: timestamp("completed_at"),
  steps: integer("steps").notNull().default(0),
  tokensUsed: integer("tokens_used").notNull().default(0),
  costUsd: real("cost_usd").notNull().default(0),
  toolCalls: jsonb("tool_calls").notNull().default([]),
});
```

From template/backend/src/ai/approval.ts:
```typescript
export type ApprovalStatus = "pending" | "approved" | "denied";
export interface ApprovalRequest {
  id: string;
  userId: string;
  tenantId: string;
  agentSessionId: string;
  toolName: string;
  toolArgs: Record<string, unknown>;
  description: string;
  status: ApprovalStatus;
  reason?: string;
  createdAt: Date;
  resolvedAt?: Date;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Approval-Schema + Agent-Session-Tracker + Rate-Limiter erstellen</name>
  <files>
    template/backend/src/db/approval-requests.schema.ts,
    template/backend/src/ai/agent-session-tracker.ts,
    template/backend/src/ai/agent-session-tracker.test.ts,
    template/backend/src/middleware/rate-limiter.ts,
    template/backend/src/middleware/rate-limiter.test.ts
  </files>
  <behavior>
    Agent Session Tracker Tests:
    - Test 1: Erster Step mit isFirstStep=true ruft db.insert auf mcAgentSessions auf
    - Test 2: Folge-Step mit isFirstStep=false ruft db.update auf mcAgentSessions auf mit SQL-Inkrement fuer steps und tokensUsed
    - Test 3: Letzter Step (finishReason="stop") setzt status="completed" und completedAt
    - Test 4: toolCalls werden als JSONB-Append via sql-Template uebergeben (nicht JS-Array-Concat)
    - Test 5: sessionId-Closure — ein Tracker-Aufruf mit isFirstStep=true fuehrt INSERT aus, ein zweiter Aufruf mit derselben sessionId und isFirstStep=false fuehrt UPDATE aus (beide durch denselben Tracker)

    Rate Limiter Tests:
    - Test 1: Requests bis zum Limit werden durchgelassen (next() wird aufgerufen)
    - Test 2: Request nach Erreichen des Limits gibt HTTP 429 zurueck mit Retry-After Header
    - Test 3: Requests nach Ablauf des Zeitfensters werden wieder durchgelassen (Sliding Window)
    - Test 4: Verschiedene Keys (verschiedene User) haben unabhaengige Limits
  </behavior>
  <action>
    **1. Approval-Schema (per D-01):**
    Erstelle `template/backend/src/db/approval-requests.schema.ts`:
    - `pgTableCreator` mit `app_` Prefix (per Phase-1 Konvention CON-02)
    - Felder exakt wie in D-01: id (text PK), tenantId, userId, toolName, toolArgs (jsonb), status (text, default "pending"), createdAt (timestamp, defaultNow), resolvedAt (timestamp nullable), resolvedBy (text nullable)
    - Export: `appApprovalRequests`, `AppApprovalRequestInsert`, `AppApprovalRequestSelect` (via $inferInsert/$inferSelect)
    - Kein Test noetig — reines Schema, wird durch DB-Migration validiert

    **2. Agent-Session-Tracker (per D-07, D-08, D-09):**
    Erstelle `template/backend/src/ai/agent-session-tracker.ts`:
    - Interface `AgentStepLogWithSession` das `AgentStepLog` aus main-agent.ts erweitert um: `sessionId: string`, `isFirstStep: boolean`, `moduleName?: string`, `channel?: string`
    - Factory `createAgentSessionTracker(db)` die eine async Funktion zurueckgibt
    - Bei `isFirstStep=true`: `db.insert(mcAgentSessions).values(...)` mit Schema-Mapping per D-09
    - Bei `isFirstStep=false`: `db.update(mcAgentSessions).set(...)` mit atomarem SQL-Inkrement:
      - `steps: sql\`${mcAgentSessions.steps} + 1\``
      - `tokensUsed: sql\`${mcAgentSessions.tokensUsed} + ${tokens}\``
      - `toolCalls: sql\`${mcAgentSessions.toolCalls} || ${JSON.stringify(newCalls)}::jsonb\``
    - Bei `finishReason === "stop"`: status="completed", completedAt=new Date()
    - Import `mcAgentSessions` ueber relativen Pfad: `../../../../modules/mission-control/backend/src/db/schema`

    Erstelle Tests mit gemocktem db-Objekt (gleiches Pattern wie bestehende approval.test.ts):
    - Mock-DB mit `insert().values()` und `update().set().where()` Chain
    - Verifiziere dass INSERT bei isFirstStep=true aufgerufen wird
    - Verifiziere dass UPDATE bei isFirstStep=false aufgerufen wird
    - Verifiziere completedAt-Setzung bei finishReason="stop"
    - **Test 5 (sessionId-Closure):** Erstelle einen Tracker, rufe ihn zweimal mit derselben sessionId auf: einmal isFirstStep=true, einmal isFirstStep=false. Verifiziere dass der erste Aufruf INSERT und der zweite UPDATE ausfuehrt. Dies beweist die korrekte Lifecycle-Handhabung.

    **3. Rate-Limiter (per D-04, D-05):**
    Erstelle `template/backend/src/middleware/rate-limiter.ts` (neues Verzeichnis `middleware/`):
    - Interface `RateLimiterConfig`: `windowMs: number`, `maxRequests: number`, `keyGenerator?: (c: Context) => string`
    - In-Memory Map `requestLog: Map<string, number[]>` (Sliding Window Timestamps)
    - Factory `createRateLimiter(config): MiddlewareHandler`
    - Key-Generierung: `userId` aus Hono Context (`c.get("userId")`) + Route-Pattern (`c.req.routePath ?? c.req.path`)
    - Bei Limit ueberschritten: `c.json({ error: "Too Many Requests" }, 429)` + `Retry-After` Header (Sekunden bis naechstes Fenster)
    - Cleanup via `setInterval` alle 5 Minuten: Keys loeschen wo alle Timestamps ausserhalb des Fensters
    - Exportiere vordefinierte Instanzen: `standardRateLimiter` (60/min), `sensitiveRateLimiter` (10/min) per D-05

    Erstelle Tests mit gemocktem Hono Context:
    - Mock `c.get("userId")`, `c.req.path`, `c.json()`, `c.header()`
    - Teste Durchlass bis Limit, Blockade danach mit Retry-After Header, Sliding Window Reset

    Variablen Englisch, Kommentare Deutsch (per CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/toby/Documents/github/projekte/super-app && bun test template/backend/src/ai/agent-session-tracker.test.ts template/backend/src/middleware/rate-limiter.test.ts</automated>
  </verify>
  <done>
    - appApprovalRequests Schema mit app_* Prefix und allen D-01 Feldern existiert
    - createAgentSessionTracker factory exportiert, Tests fuer INSERT/UPDATE/completedAt/sessionId-Closure gruen
    - createRateLimiter + standardRateLimiter + sensitiveRateLimiter exportiert, Tests fuer 429/Retry-After/Sliding-Window gruen
  </done>
</task>

</tasks>

<verification>
- Alle 3 neuen Produktionsdateien existieren und exportieren die definierten Interfaces
- Alle Tests gruen: `bun test template/backend/src/ai/agent-session-tracker.test.ts template/backend/src/middleware/rate-limiter.test.ts`
- Kein bestehender Code geaendert (rein additive Aenderungen)
</verification>

<success_criteria>
- appApprovalRequests Schema erstellt mit korrektem app_* Prefix
- Agent Session Tracker mit INSERT/UPDATE-Pattern und atomarem JSONB-Append
- sessionId-Closure Test beweist korrekten INSERT→UPDATE Lifecycle
- Rate Limiter mit Sliding Window, 429 Response, Retry-After Header, Cleanup
- Alle Tests bestanden
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-system-completion/03-01-SUMMARY.md`
</output>
